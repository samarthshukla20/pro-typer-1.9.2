<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Dark Mode: White border -->
    <item android:color="@android:color/white" android:state_enabled="true" android:theme="@android:style/Theme.DeviceDefault" />
    
    <!-- Light Mode: Black border -->
    <item android:color="@android:color/black" android:state_enabled="true" android:theme="@android:style/Theme.Light" />
    
    <!-- Default (for unfocused or any other condition) -->
    <item android:color="@android:color/black"/>
</selector>